# Normalize line endings to LF in the repository
* text=auto eol=lf

# Shell scripts must stay LF (executable on Linux/container)
*.sh text eol=lf

# Windows-only files
*.bat text eol=crlf
*.cmd text eol=crlf
